

const CalculateSquareArea = ({ length }) => {
    const area = length * length; // Fórmula del área de un cuadrado (lado * lado)
    
    return <h2>El área del cuadrado es: {area}</h2>;
  };
  
  export default CalculateSquareArea;